import { 
    GET_BREEDS, 
    GET_BREED_NAME,
    GET_TEMPERAMENTS, 
    ADD_BREED,
    UPDATE_BREED,
    SEARCH_BREED,
    GET_DETAIL,
    EMPTY_DETAIL,
    DELETE_BREED,
    FILTER_BY_CREATED,
    FILTER_BY_TEMPERAMENT,
    RESET_FILTER,
    ORDER_BY_WEIGHT,
    ORDER_BY_HEIGHT,
    ORDER_BY_NAME,
    RESET_ORDER,
    UPDATE_CHANGE,
    SET_ACTUAL_PAGE,
    SET_CURRENT_BUTTONS
}


const initialState = {

}


export default function rootReducer(state=initialState, action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}